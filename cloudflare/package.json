{
  "name": "crc-transcriptomics-dashboard-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker + Container that serves dashboard/app.py (Streamlit) via Cloudflare Containers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "typescript": "^5.7.0",
    "wrangler": "^4.106.0"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  }
}
